---
import projectsJson from "@/assets/projects/projects.json";
import ProjectCard from "@/components/project-card.astro";
import Layout from "@/layouts/layout.astro";
---

<Layout
  title="Projects • Charles Wang"
  description="Coding stuff. Projects from my classes and random interests."
>
  <main
    class="flex min-h-[calc(100vh-105px)] flex-col items-center px-7 text-sweater-1"
  >
    <p class="mb-7 w-full max-w-[850px]">
      I try to make all my projects open source. Stuff that doesn&apos;t have a
      link is probably from a class and I don&apos;t have permission to share
      it. Everything else is public and can be found on my GitHub.
    </p>
    <ul class="flex max-w-[1100px] flex-col space-y-7">
      {
        projectsJson.map((project, index) => (
          <ProjectCard
            project={project}
            index={index}
          />
        ))
      }
    </ul>
  </main>
</Layout>
