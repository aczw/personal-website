---
import { type HTMLAttributes } from "astro/types";
import { Hash } from "lucide-astro";

type Props = HTMLAttributes<"h3">;

const { id, ...props } = Astro.props;
---

<h3
  id={id}
  class="pt-4 text-sweater-2"
  {...props}
>
  <a
    href={`#${id}`}
    class="group relative -ml-2 flex w-fit items-center gap-1.5 pl-2"
  >
    <slot />
    <Hash
      size={14}
      class="stroke-transparent transition-colors group-hover:stroke-sweater-5 wide:absolute wide:-left-4"
      aria-hidden="true"
    />
  </a>
</h3>
