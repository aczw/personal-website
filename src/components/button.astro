---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<button
  class={`bg-sweater-4 rounded-full px-3 py-1 hover:bg-sweater-3 transition-colors ${className}`}
  ><slot /></button
>
