---
import { cn } from "@/utils/cn";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<main
  class={cn(
    "px-3 pb-12 pt-[calc(theme(spacing.header)+theme(spacing.10))]",
    className,
  )}
>
  <slot />
</main>
